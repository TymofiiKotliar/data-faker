# seed.py

## Running the Seed
To run the seed script, use:

```bash
python seed.py
```

This will execute the seeding script to populate your database.

# Using the Seeded Data
To use the seeded data, you can import seed.sql to your databese. This will create the necessary tables and insert the seeded data.
